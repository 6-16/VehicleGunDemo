public readonly struct SpawnPoint
{
    public readonly float Distance;
    public readonly float Lateral;

    public SpawnPoint(float distance, float lateral)
    {
        Distance = distance;
        Lateral = lateral;
    }
}
